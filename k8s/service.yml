apiVersion: v1
kind: Service
metadata:
  name: eks-version-exporter
  labels:
    app: eks-version-exporter
spec:
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
  selector:
    app: eks-version-exporter
